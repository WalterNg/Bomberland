# Bomberland Project

run:
```
pip install -r requirements.txt
```

#### Headless testing
```
PYTHONPATH=. python3 submission_kit/run_match.py --num_episodes 100
```

#### Simple Visualizer
```
PYTHONPATH=. python3 visualizer/viewer.py --num_episodes 10
```
(stay at Bomberland/)

Currently:
- Random is a noobie
- Smarter vs Simple -> never ends (both are shy)
- Smarter is lol smarter than Genius
- Genius beats Simple (and ofc beats the heck out of Random)